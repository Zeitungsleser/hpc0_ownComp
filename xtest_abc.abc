@ <stdio.hdr>
@ "parser.hdr"
@ "lexer.hdr"
@ "genvar.hdr"

fn main()
{
    genInit();
    getToken();
    while(parseFunction()) {
    }
    if (token.kind != EOI) {
        printf("%u.%u: syntax error\n", token.pos.row, token.pos.col);
    }
    genPrintSymtab();
}
